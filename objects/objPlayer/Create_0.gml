gravForce = 0.2;
maxGravForce = 2;

velocity = new Vector2(0, 0);
//imageScale = new Vector2(1, 1);

slowTimeFull = 1.5 * FRAMES;
slowTime = slowTimeFull;

shootTimerFull = 0.5 * FRAMES;
shootTimer = shootTimerFull;

fireTimer = 8;
alarm[4] = fireTimer;

biggerY = ystart;

global.timerActive = true;